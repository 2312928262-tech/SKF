import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const read=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');
test('Tauri connects thin CLI bridge, not a second core, with management protocol allowlist',()=>{const s=read('src-tauri/src/supervisor.rs');assert.match(s,/join\("cli.js"\)/);assert.match(s,/\.arg\("bridge"\)/);assert.ok(!s.includes('.arg("--ipc")'));assert.ok(s.includes('"management"'));assert.ok(!s.includes('.env("SKF_DATA_DIR", &data)'));});
test('model settings provide CRUD, hidden replace-only credentials, no browser persistence or raw HTML interpolation',()=>{const s=read('ui-preview/model-settings.js');for(const p of ['/v1/setup/status','/v1/setup/commit','/v1/setup/test','/v1/setup/discover','/v1/providers','/v1/models/default'])assert.ok(s.includes(p));assert.ok(s.includes("'password'"));assert.ok(!/localStorage|sessionStorage|innerHTML/.test(s));assert.ok(s.includes('expectedRevision'));assert.ok(s.includes('body.key = undefined'));assert.ok(s.includes('key.input.value ='));assert.ok(s.includes('不等于工具授权'));assert.ok(s.includes('已有会话'));});
